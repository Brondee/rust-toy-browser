use std::collections::HashMap;

pub mod css;
pub mod dom;
pub mod html;

fn main() {
    println!("Hello, world!");

    // let mut attrs: HashMap<String, String> = HashMap::new();
    // attrs.insert("class".to_string(), "my-class".to_string());

    // let children: Vec<dom::Node> = Vec::new();

    // let first_elem: dom::Node = dom::elem(String::from("main"), attrs, children);

    // println!("{:#?}", first_elem);
}
